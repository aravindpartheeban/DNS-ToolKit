# DNS-ToolKit
DNS Toolkit is a lightweight Windows utility that benchmarks DNS latency and allows quick switching between trusted DNS providers. It supports privacy-focused, gaming, security, family-safe, and ad-blocking resolver profiles, and applies DNS changes using native Windows networking commands. No telemetry. Portable executable.
